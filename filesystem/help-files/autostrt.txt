
SYNOPSIS
    autostart
DESCRIPTION
    initialize the autostart routine. 
    the autostart mode has to be "auto" and
    the microcontroller can not be in error 004 state.
    This command will also shorten any currently occuring
    timeout to 3 seconds.

